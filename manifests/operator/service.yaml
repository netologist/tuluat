apiVersion: v1
kind: Service
metadata:
  name: tuluat-operator-service
  namespace: tuluat-system
spec:
  selector:
    app: tuluat-operator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
