apiVersion: v1
kind: Service
metadata:
  name: k8s-ai-operator-service
  namespace: default
spec:
  selector:
    app: k8s-ai-operator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
